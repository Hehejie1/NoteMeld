import { useState } from 'react'
import CloudClient, { CloudUser } from '../../services/cloud'

export function CloudUserForm({ client, user, onSaved }: { client: CloudClient; user?: CloudUser; onSaved?: () => void }) {
  const [username, setUsername] = useState(user?.username ?? ''); const [password, setPassword] = useState(''); const [disabled, setDisabled] = useState(user?.disabled ?? false); const [busy, setBusy] = useState(false); const [error, setError] = useState<string | null>(null)
  async function submit(event: React.FormEvent) { event.preventDefault(); setBusy(true); setError(null); try { if (user) await client.updateUser(user.id, { username, ...(password ? { password } : {}), disabled }); else await client.createUser(username, password); setPassword(''); onSaved?.() } catch (reason) { setError(reason instanceof Error ? reason.message : 'Unable to save user') } finally { setBusy(false) } }
  return <form onSubmit={submit} aria-labelledby="user-form-title" className="flex max-w-md flex-col gap-3 p-3"><h2 id="user-form-title" className="text-base font-semibold">{user ? 'Edit cloud user' : 'Add cloud user'}</h2><label htmlFor="cloud-user-name" className="text-sm font-medium">Username</label><input id="cloud-user-name" required value={username} onChange={event => setUsername(event.target.value)} className="rounded border border-slate-300 px-3 py-2" /><label htmlFor="cloud-user-password" className="text-sm font-medium">{user ? 'New password (optional)' : 'Password'}</label><input id="cloud-user-password" type="password" autoComplete="new-password" required={!user} value={password} onChange={event => setPassword(event.target.value)} className="rounded border border-slate-300 px-3 py-2" />{user ? <label className="flex items-center gap-2 text-sm"><input type="checkbox" checked={disabled} onChange={event => setDisabled(event.target.checked)} /> Disabled</label> : null}<button type="submit" disabled={busy || !username || (!user && !password)} className="rounded bg-slate-900 px-3 py-2 text-sm text-white disabled:opacity-50">{busy ? 'Saving…' : 'Save user'}</button>{error ? <p role="alert" className="text-sm text-red-700">{error}</p> : null}</form>
}
