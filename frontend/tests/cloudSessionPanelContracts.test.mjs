import assert from 'node:assert/strict'
import fs from 'node:fs'
import test from 'node:test'

const panel = fs.readFileSync(new URL('../src/components/CloudSessionPanel/CloudSessionPanel.tsx', import.meta.url), 'utf8')
const list = fs.readFileSync(new URL('../src/components/CloudSessionList/CloudSessionList.tsx', import.meta.url), 'utf8')
const devices = fs.readFileSync(new URL('../src/components/CloudDeviceList/CloudDeviceList.tsx', import.meta.url), 'utf8')
const login = fs.readFileSync(new URL('../src/components/CloudLoginPanel/CloudLoginPanel.tsx', import.meta.url), 'utf8')
const pairing = fs.readFileSync(new URL('../src/components/CloudPairingPanel/CloudPairingPanel.tsx', import.meta.url), 'utf8')
const grant = fs.readFileSync(new URL('../src/components/CloudGrantPanel/CloudGrantPanel.tsx', import.meta.url), 'utf8')
const approval = fs.readFileSync(new URL('../src/components/CloudApprovalList/CloudApprovalList.tsx', import.meta.url), 'utf8')
const workspace = fs.readFileSync(new URL('../src/components/CloudWorkspaceBrowser/CloudWorkspaceBrowser.tsx', import.meta.url), 'utf8')
const models = fs.readFileSync(new URL('../src/components/CloudModelList/CloudModelList.tsx', import.meta.url), 'utf8')
const modelForm = fs.readFileSync(new URL('../src/components/CloudModelForm/CloudModelForm.tsx', import.meta.url), 'utf8')
const users = fs.readFileSync(new URL('../src/components/CloudUserList/CloudUserList.tsx', import.meta.url), 'utf8')
const userForm = fs.readFileSync(new URL('../src/components/CloudUserForm/CloudUserForm.tsx', import.meta.url), 'utf8')
const share = fs.readFileSync(new URL('../src/components/CloudSharePanel/CloudSharePanel.tsx', import.meta.url), 'utf8')
const cloudPage = fs.readFileSync(new URL('../src/pages/CloudPage/index.tsx', import.meta.url), 'utf8')
const app = fs.readFileSync(new URL('../src/App.tsx', import.meta.url), 'utf8')
test('cloud session panel has loading, archive, action errors, and accessible event states', () => { assert.match(panel, /aria-busy/); assert.match(panel, /actionBusy/); assert.match(panel, /actionError/); assert.match(panel, /role="alert"/); assert.match(panel, /No events yet/); assert.match(panel, /role="log"/); assert.match(panel, /Copy branch/); assert.match(panel, /controller\.archive/); assert.match(panel, /Message/) })
test('cloud session list supports archive filtering, restore, hard delete, and keyboard selection', () => { assert.match(list, /listSessions\(archived\)/); assert.match(list, /onRestore/); assert.match(list, /onDelete/); assert.match(list, /Delete permanently/); assert.match(list, /aria-current/); assert.match(list, /role="list"/); assert.match(list, /No archived sessions/) })
test('device list exposes online status, LAN candidates, revoke action, polling, and failure handling', () => { assert.match(devices, /device\.online/); assert.match(devices, /lan_endpoints/); assert.match(devices, /revokeDevice/); assert.match(devices, /setBusy/); assert.match(devices, /setInterval/); assert.match(devices, /clearInterval/); assert.match(devices, /role="alert"/); assert.match(devices, /aria-label="Connected devices"/) })
test('cloud login panel uses username and password only', () => { assert.match(login, /autoComplete="username"/); assert.match(login, /autoComplete="current-password"/); assert.match(login, /client\.login\(password, username\)/); assert.doesNotMatch(login, /email|phone|手机号|邮箱/i) })
test('pairing panel uses short-lived cloud pairing flow', () => { assert.match(pairing, /startPairing/); assert.match(pairing, /confirmPairing/); assert.match(pairing, /aria-label="Pairing code"/); assert.match(pairing, /role="alert"/) })
test('grant panel defaults to standard non-dangerous scopes', () => { assert.match(grant, /createGrant/); assert.match(grant, /message\.send/); assert.match(grant, /Dangerous actions still require host approval/); assert.doesNotMatch(grant, /full_access/) })
test('approval list requires explicit approve or reject actions', () => { assert.match(approval, /listApprovals/); assert.match(approval, /resolveApproval/); assert.match(approval, /Approve/); assert.match(approval, /Reject/); assert.match(approval, /No pending approvals/) })
test('workspace browser is read-only and never exposes write or delete actions', () => { assert.match(workspace, /listWorkspaceFiles/); assert.match(workspace, /readWorkspaceFile/); assert.match(workspace, /Read-only preview/); assert.doesNotMatch(workspace, /writeWorkspaceFile|deleteWorkspaceFile/) })
test('model list displays only provider metadata and credential presence', () => { assert.match(models, /listModels/); assert.match(models, /has_api_key/); assert.doesNotMatch(models, /api_key_ciphertext|api_key["']?\s*:/) })
test('model form treats API key as password input and clears it after save', () => { assert.match(modelForm, /type="password"/); assert.match(modelForm, /createModel/); assert.match(modelForm, /setApiKey\('\'\)/); assert.match(modelForm, /autoComplete="new-password"/) })
test('admin user list protects admin and supports user deletion', () => { assert.match(users, /listUsers/); assert.match(users, /deleteUser/); assert.match(users, /user\.role === 'admin'/); assert.match(users, /Protected/) })
test('admin user form supports create and edit without retaining passwords', () => { assert.match(userForm, /createUser/); assert.match(userForm, /updateUser/); assert.match(userForm, /type="password"/); assert.match(userForm, /setPassword\('\'\)/) })
test('share panel creates viewer tokens and supports revocation', () => { assert.match(share, /createShareToken/); assert.match(share, /revokeShareToken/); assert.match(share, /event\.receive/); assert.match(share, /will not be shown again/) })
test('cloud page exposes login, session creation, archive actions, sharing, approvals, workspace, devices, models, and responsive layout route', () => { assert.match(cloudPage, /CloudLoginPanel/); assert.match(cloudPage, /createSession\('cloud_native'\)/); assert.match(cloudPage, /restoreSession/); assert.match(cloudPage, /deleteSession/); assert.match(cloudPage, /archiveAction/); assert.match(cloudPage, /CloudSessionList/); assert.match(cloudPage, /CloudSessionPanel/); assert.match(cloudPage, /CloudSharePanel/); assert.match(cloudPage, /CloudApprovalList/); assert.match(cloudPage, /CloudWorkspaceBrowser/); assert.match(cloudPage, /CloudDeviceList/); assert.match(cloudPage, /CloudPairingPanel/); assert.match(cloudPage, /CloudGrantPanel/); assert.match(cloudPage, /CloudModelList/); assert.match(cloudPage, /CloudModelForm/); assert.match(cloudPage, /max-md:grid-cols-1/); assert.match(app, /path="\/cloud"/) })
test('cloud page bootstraps encrypted IndexedDB token storage', () => { const tokenStore = fs.readFileSync(new URL('../src/services/tokenStore.ts', import.meta.url), 'utf8'); assert.match(cloudPage, /openIndexedDbTokenStore/); assert.match(tokenStore, /generateKey\(\{ name: 'AES-GCM'/); assert.match(tokenStore, /extractable|false/) })
test('cloud page gates user administration to admins', () => { assert.match(cloudPage, /auth\.role === 'admin'/); assert.match(cloudPage, /CloudUserList/); assert.match(cloudPage, /CloudUserForm/) })
