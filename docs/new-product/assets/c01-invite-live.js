(()=>{
  const invite=new URLSearchParams(location.search).get('invite');if(!invite)return;
  const node=document.createElement('div');node.className='modal-backdrop';node.innerHTML='<section class="create-dialog" role="dialog" aria-modal="true" aria-labelledby="invite-accept-title"><header><div><span class="section-kicker">Cloud invitation</span><h2 id="invite-accept-title">接受云端邀请</h2><p>设置你的显示名称和登录密码后加入 NoteMeld 云端。</p></div></header><form class="auth-form" data-invite-accept><label>显示名称<input name="display_name" autocomplete="name" required></label><label>登录密码<input name="password" type="password" minlength="8" autocomplete="new-password" required></label><p class="form-message" data-invite-message role="status"></p><div class="dialog-actions"><button class="primary-button" type="submit">接受邀请</button></div></form></section>';document.body.append(node);const form=node.querySelector('form'),message=form.querySelector('[data-invite-message]');form.addEventListener('submit',async event=>{event.preventDefault();const button=form.querySelector('[type="submit"]');button.disabled=true;message.textContent='正在创建账号…';message.className='form-message info';try{const response=await fetch('/v1/invitations/'+encodeURIComponent(invite)+'/accept',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({display_name:form.elements.display_name.value.trim(),password:form.elements.password.value})}),payload=await response.json();if(!response.ok)throw new Error(payload.detail||'邀请无效或已过期');message.textContent='邀请已接受，请使用邮箱和密码登录。';message.className='form-message info';const account=document.querySelector('[data-login-form] input[name="account"]');if(account)account.value=payload.data.email;setTimeout(()=>node.remove(),900)}catch(error){message.textContent=error.message;message.className='form-message';button.disabled=false}});
})();
