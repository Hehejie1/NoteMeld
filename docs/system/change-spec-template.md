# Change Spec Template

更新时间：2026-06-19

所有新需求都必须先产出增量 Change Spec，禁止孤立 PRD。Change Spec 必须基于当前系统事实说明“在现有系统上如何最小改动”，不能只写理想方案。

复制本模板到合适位置后填写。未答清的问题必须继续查代码、查文档、查测试，或向用户提问。

## 0. 预检查

- [ ] 已阅读 `docs/system/current-architecture.md`
- [ ] 已阅读 `docs/system/product-rules.md`
- [ ] 已阅读 `docs/system/data-model.md`
- [ ] 已阅读 `docs/system/api-inventory.md`
- [ ] 已阅读 `docs/system/known-pitfalls.md`
- [ ] 已搜索相关代码
- [ ] 已搜索相关测试
- [ ] 已确认是否存在类似能力
- [ ] 已确认是否影响本地数据或线上/发布服务

## 1. 当前系统现状

说明当前真实实现，不写目标态。

- 相关模块：
- 相关入口：
- 相关数据表/文件：
- 相关 API：
- 相关前端页面/组件：
- 相关测试：
- 当前行为：
- 当前限制：

## 2. 本次目标

用可验证语言描述本次要达成的增量变化。

- 用户问题：
- 成功后的用户可见行为：
- 成功后的系统内部行为：
- 必须保留的旧行为：

## 3. 明确不做

列出本次不做的范围，防止无关重构和范围膨胀。

- 不做：
- 暂不处理：
- 后续可选：

## 4. 冲突分析

逐项检查是否冲突。

- 是否和 `product-rules.md` 冲突：
- 是否和 `data-model.md` 字段语义冲突：
- 是否和 `api-inventory.md` 接口契约冲突：
- 是否会重新引入 `known-pitfalls.md` 的问题：
- 是否影响桌面/源码/CLI/MCP 任一运行模式：
- 是否影响打包、迁移、导入、回滚：

## 5. 影响范围

- 后端文件：
- 前端文件：
- 桌面/Tauri 文件：
- 数据库模型/迁移：
- 文件系统结构：
- API 调用方：
- MCP 工具：
- 测试：
- 文档：

## 6. 实施方案

描述最小可行改动。优先复用现有模块和模式。

### 后端

- 改动点：
- 错误处理：
- 日志/可观测性：
- 并发/取消/超时：

### 前端

- 改动点：
- 状态流转：
- 加载/错误/空状态：
- 桌面 ready gate：

### 桌面/打包

- 改动点：
- 发布影响：

## 7. 数据变更

- 是否新增/修改 SQLite 表：
- 是否新增/修改字段：
- 是否新增/修改文件结构：
- 是否需要迁移：
- 是否影响历史数据：
- 是否需要清理脏数据：
- 回滚后数据如何处理：

## 8. 接口变更

- 新增接口：
- 修改接口：
- 删除接口：
- 请求参数变化：
- 返回结构变化：
- 错误语义变化：
- 前端调用方：
- MCP 调用方：
- 兼容策略：
- 是否更新 `docs/system/api-inventory.md`：

## 9. UI/交互变更

- 页面/组件：
- 用户操作路径：
- 加载态：
- 空状态：
- 失败态：
- 成功态：
- 可访问性：
- 是否影响桌面与浏览器差异：

## 10. 测试方案

必须说明如何防止回归。

- 后端单测/契约测试：
- 前端契约测试：
- 构建/类型检查：
- 手动验证：
- 原问题复现方式：
- 回归断言：

## 11. 验收标准

每条验收标准必须可检查。

- [ ] 标准 1：
- [ ] 标准 2：
- [ ] 标准 3：

## 12. 风险和回滚

- 主要风险：
- 风险触发信号：
- 降级策略：
- 回滚步骤：
- 回滚后数据一致性：
- 用户可见影响：

## 13. Agent 必答问题

任何新需求开始前，Agent 必须回答：

- 这个需求影响哪些已有模块？
- 当前系统是否已有类似能力？
- 是否和产品规则冲突？
- 是否和数据模型字段语义冲突？
- 是否会重新引入 known-pitfalls 中的问题？
- 是否影响本地数据或线上服务？
- 最小可行改动是什么？
- 需要补哪些测试防止回归？

如果答不清楚，必须继续查代码、查文档、查测试，或者向用户提问，不能猜。
