# official.link-note

The official NoteMeld link capability. It accepts the frozen N01 URL platform
set and delegates persistence, progress, cancellation, and recovery to host
ports. The package contains no application router, Conversation, Event, or
SQLite implementation.
